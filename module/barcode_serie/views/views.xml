<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="barcode_serie.list">
      <field name="name">barcode_serie list</field>
      <field name="model">barcode_serie.barcode_serie</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="barcode_serie.action_window">
      <field name="name">barcode_serie window</field>
      <field name="res_model">barcode_serie.barcode_serie</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="barcode_serie.action_server">
      <field name="name">barcode_serie server</field>
      <field name="model_id" ref="model_barcode_serie_barcode_serie"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="barcode_serie" id="barcode_serie.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="barcode_serie.menu_1" parent="barcode_serie.menu_root"/>
    <menuitem name="Menu 2" id="barcode_serie.menu_2" parent="barcode_serie.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="barcode_serie.menu_1_list" parent="barcode_serie.menu_1"
              action="barcode_serie.action_window"/>
    <menuitem name="Server to list" id="barcode_serie" parent="barcode_serie.menu_2"
              action="barcode_serie.action_server"/>
    -->
  </data>
</odoo>